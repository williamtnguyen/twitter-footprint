<%- include('partials/header') %>

    <div id="landingPage">
        <div id="header">
            <h1>you have tweeted <span id="numTweets">0</span> tweets</h1>
        </div>
    </div>

    <script>
        const countUp = new CountUp("numTweets", 0, numTweets);
        if (!countUp.error) {
            countUp.start();
        } else {
            console.error(countUp.error);
        }
    </script>

    <script src="https://cdn.jsdelivr.net/npm/countup@1.8.2/dist/countUp.min.js"></script> 
<%- include('partials/footer') %>